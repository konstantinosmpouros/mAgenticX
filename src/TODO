# TODO

## General

- Plug tool in FastAPI that auto-generates documentation for all available APIs (/docs)
- In the mobile view the sidebar doesn't exist cause its too shrunk.

## Agentic UI

- Give in the agents the personal info of the user in the prompt like name and role (langchain context)
- In the session persistence the current conversation is not save properly any more cause of the available agent functionality we added.

- Make the title creation functionality for a new conversation as bg task and in the end, update the conversation db, and ui page header
- Implement a tab in the sidebar to show all the images, like openai library
- Create one more chat container similar to gemini code or cursor (agentic chat)
- Reactive theme change buttons, [integrate it!](https://www.shadcn.io/button/theme-toggle)

## Agents

- Improve the current agent's architecture
- Maybe the [Runtime Context](https://docs.langchain.com/oss/python/langgraph/graph-api) is the answer to personalization, long term memory and many more.
- Implement a long-term memory service that keep memories from all agents (e.g., episodic)
- Create a deep research agent with deep agents and make it available
- Replace tools with mcp in a different directory than agents. Configure the server with all the tools and the registry will be stored in the main.py in agents and expose an API so the UI can get the available tools.
- Remove the user_input_json cause it produces exception in the api call to openai. Concatenate it with the template of each agent.
- Give in the agents the personal info of the user in the prompt like name and role
- Update the retrieval process and the whole RAG pipelines

## Dialogue Bridge

- Create an API to process the files when the user uploads them, and chunk them, not append them in any vector store yet.
- Create the functionality of doc description and add it to the db as well as a column.
- Fix the generate title api call.
- For the attachment parsing we can create a collection in the vector store with the conv id and chunk the docs and retrieve them from there and in the message contain also a description of the document
- Give in the agents the personal info of the user in the prompt like name and role (langchain context)
- Create a get_available_tools API for the UI and send the configs for the inference too.
