# TODO

## General

- Plug tool in FastAPI that auto-generates documentation for all available APIs (/docs)
- Create flow diagrams so a viewer of the repo can understand fast what is happening
- Check what is hydra

## Agentic UI

- Implement the API call for the agentic inference with AG-UI
- When downloading something large for some reason it stuck even though the backend has return status 200
- Make the title creation functionality for a new conversation a bg task and in the end, update the conversation db, and ui.
- Implement the interrupt inference process with the signal and change the send icon to stop
- The streaming works well but no use of ag-ui actual package
- Clean the inference code, handler, agui event handling, types etc
- In the tool call start event l don't see the name of the tool

- Implement a tab in the profile panel to show all the images, like openai library
- Enhance the profile panel with actual user info
- Enhance the agent message with like and dislike
- Check what to do with the audio input

## Agents

- Implement a long-term memory service that keep memories from all agents (e.g., episodic)
- Make all the agents support images pls
- Create a deep research agent with deep agents and make it available

## Dialogue Bridge

- Implement tha agentic inference api call with AG-UI
- Implement the like and dislike endpoints for messages
- Instead of checking all the time if the agent exists in the database in the lifespan implemented once the retrieval and use a variable to have search faster
- Fix and uncomment the generate title api call cause in 2-3 images it hit time out exception
- For the attachment parsing we can create a collection in the vector store with the conv id and chunk the docs and retrieve them from there and in the message contain also a description of the document
