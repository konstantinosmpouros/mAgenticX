# TODO

## In Progress

- Improve the current agent's architecture & remove the user_input_json cause it produces exception in the api call to openai. Concatenate it with the template of each agent.

## New Features

- Place some suggestions below the input container and if the user write something then remove them. Make this suggestions configurable from the fronted and personalized per user according to the latest conversations and memory we have from him (must come after the long-term memory). Dont use an llm but use embeddings and similarity search to find the latest topics discussed with the user by using a number of predefined questions/queries.
- Implement a long-term memory service that keep memories from all agents (e.g., episodic, procedural).

## Agents

- Update the retrieval process and the whole RAG pipelines so that it will be like an mcp tool calling.
- Check if instead of a whole dynamic rag system per conversation it can be replaced with the virtual file system of the deep agents.

## Agentic UI

- Check what is this and how it can simplify the [react code](https://www.shadcn.io/hooks/use-boolean)
- Use this for the approval of a [HITL Event](https://ai-sdk.dev/elements/components/confirmation) or create a custom one.
- This can be used for the agentic chat stream feel as [actions](https://elements.ai-sdk.dev/components/task) or [this one](https://elements.ai-sdk.dev/components/tool).
- Voice mode [react element](https://elements.ai-sdk.dev/components/persona).

## Dialogue Bridge

- Create an API to process the files when the user uploads them, and chunk them, not append them in any vector store yet.
- Create the functionality of doc description and add it to the db as well as a column.
- For the attachment parsing we can create a collection in the vector store with the conv id and chunk the docs and retrieve them from there and in the message contain also a description of the document.

## Code Quality

- Check the naming conventions and make them consistent across the codebase.
- Add inline documentation for all functions and classes.
