# Agentic UI Tests

This folder contains repository-level frontend contract tests.

The current pytest coverage checks API route, CSRF, package, and shared alias
invariants for the React client. Component and browser-level tests should be
added when a JavaScript test runner is introduced.
