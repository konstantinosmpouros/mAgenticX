# Integration Tests

This folder is for backend integration flows that exercise multiple API areas together with deterministic service doubles.

These are not browser-level end-to-end tests. Browser E2E coverage belongs under `tests/agentic_ui/e2e` once Playwright or an equivalent runner is introduced.
