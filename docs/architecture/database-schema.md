# Database Schema

This document describes the bridge's **`chat_db`** database (the render + relational store), accessed through an async SQLAlchemy engine backed by asyncpg. Its schema is defined in `src/dialogue_bridge/core/database.py` via SQLAlchemy ORM models and **versioned with Alembic** — migration files live in `src/dialogue_bridge/migrations/versions/` (`0001_baseline` → `0007_checkpoint_lineage`), applied via `alembic upgrade head` automatically on bridge startup (see the CLAUDE.md schema-change workflow). A separate **`agent_runtime`** database on the same Postgres instance is owned by the agents service (LangGraph `AsyncPostgresSaver` checkpoints); it is not part of these ORM models and is created/migrated by the saver's own `setup()`. All primary keys are UUIDs generated by a server-side `gen_uuid()` helper. All timestamps are timezone-naive UTC. `updated_at` columns are maintained via SQLAlchemy's `onupdate=func.now()`.

---

## Entity Relationship Diagram

```mermaid
erDiagram
    users ||--o{ conversations : "owns"
    users ||--o| user_preferences : "has"
    users ||--o{ sessions : "has"
    agents ||--o{ conversations : "used by"
    conversations ||--o{ messages : "contains"
    conversations ||--o| conversation_reports : "flagged by"
    conversations ||--o{ conversation_shares : "shared via"
    conversations }o--o| messages : "active_assistant_message_id"
    messages ||--o{ messages : "parent_message_id (branching)"
    messages ||--o{ attachments : "has"
    messages }o--o| conversations : "forked_message_id"
    conversations }o--o| conversations : "forked_parent_id"
    attachments ||--o| blobs : "stored in"

    users {
        string id PK
        string username UK
        string vault_user_id UK
        string email UK
        string display_name
        string avatar_url
        string full_name
        string department
        string role_title
        datetime last_login_at
        boolean is_active
        datetime created_at
        datetime updated_at
    }

    sessions {
        string id PK
        string user_id FK
        string access_token_hash UK
        string refresh_token_hash UK
        datetime access_expires_at
        datetime refresh_expires_at
        datetime revoked_at
        string user_agent_hash
        string ip_hash
        datetime last_used_at
        datetime last_refreshed_at
        datetime created_at
        datetime updated_at
    }

    agents {
        string id PK
        string slug UK
        string name
        string description
        string icon
        string version
        boolean is_active
        datetime created_at
        datetime updated_at
    }

    conversations {
        string id PK
        string user_id FK
        string agent_id FK
        string forked_parent_id FK
        string forked_message_id FK
        string active_assistant_message_id FK
        string agent_name
        string title
        boolean is_private
        boolean is_archived
        datetime archived_at
        boolean is_reported
        datetime reported_at
        string last_message_preview
        datetime last_message_at
        datetime created_at
        datetime updated_at
    }

    messages {
        string id PK
        string conversation_id FK
        string parent_message_id FK
        enum sender
        text content
        boolean liked
        string agent_id FK
        string agent_name
        json reasoning_steps
        int reasoning_time_seconds
        int input_tokens
        int output_tokens
        string checkpoint_thread_id
        string checkpoint_id
        boolean is_error
        text error_message
        json raw_events
        string streaming_status
        json streaming_message_path
        json streaming_enabled_tools
        datetime streaming_started_at
        datetime streaming_completed_at
        datetime streaming_cancel_requested_at
        datetime created_at
        datetime updated_at
    }

    attachments {
        string id PK
        string message_id FK
        string blob_id FK
        string file_name
        string mime_type
        int size_bytes
        datetime created_at
        datetime updated_at
    }

    blobs {
        string id PK
        bytea data
        datetime created_at
    }

    user_preferences {
        string id PK
        string user_id FK_UK
        json tools
        boolean prefers_agentic_chat
        boolean suggestions_enabled
        string voice_mode_voice
        string voice_mode_language
        datetime updated_at
    }

    conversation_reports {
        string id PK
        string conversation_id FK_UK
        string user_id FK
        string message_id FK
        string reason
        text details
        string status
        datetime created_at
        datetime updated_at
    }

    conversation_shares {
        string id PK
        string token UK
        string conversation_id FK
        string owner_user_id FK
        string snapshot_until_message_id FK
        string title
        json snapshot_json
        boolean is_active
        datetime revoked_at
        datetime expires_at
        datetime created_at
        datetime updated_at
    }
```

---

## Table Reference

### `users`

The identity record for every person who has logged in. Created (or refreshed) on first successful Vault login via `upsert_user_from_vault()`.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `username` | `String` | No | — | UNIQUE, INDEXED — Vault username |
| `vault_user_id` | `String` | No | — | UNIQUE, INDEXED — Vault `entity_id`, stable across password changes |
| `email` | `String` | Yes | `NULL` | UNIQUE, INDEXED — may be absent if Vault metadata is empty |
| `display_name` | `String` | Yes | `NULL` | From Vault metadata |
| `avatar_url` | `String` | Yes | `NULL` | From Vault metadata |
| `full_name` | `String` | Yes | `NULL` | From Vault metadata |
| `department` | `String` | Yes | `NULL` | From Vault metadata |
| `role_title` | `String` | Yes | `NULL` | From Vault metadata |
| `last_login_at` | `DateTime` | Yes | `NULL` | Updated on every successful login, before session creation |
| `is_active` | `Boolean` | No | `true` | Set to `false` to ban a user at the application layer without touching Vault |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Relationships:** `preferences` (one-to-one, cascade delete), `sessions` (one-to-many, cascade delete), `conversations` (one-to-many, cascade delete).

---

### `sessions`

One row per browser session. Tokens are never stored raw — only HMAC-SHA256 hashes keyed by `TOKEN_SECRET`. See [authentication-and-session.md](../flows/authentication-and-session.md) for the full session lifecycle.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `user_id` | `String` | No | — | FK → `users.id` CASCADE, INDEXED |
| `access_token_hash` | `String` | No | — | UNIQUE, INDEXED — HMAC hash of the `mx_session` cookie value |
| `refresh_token_hash` | `String` | No | — | UNIQUE, INDEXED — HMAC hash of the `mx_refresh` cookie value |
| `access_expires_at` | `DateTime` | No | — | UTC expiry for the access token (`ACCESS_TTL_SECONDS` from login or last refresh) |
| `refresh_expires_at` | `DateTime` | No | — | UTC expiry for the refresh token (`REFRESH_TTL_SECONDS`) |
| `revoked_at` | `DateTime` | Yes | `NULL` | Set on logout; row kept for audit trail |
| `user_agent_hash` | `String` | Yes | `NULL` | HMAC hash of `User-Agent` at login — stored for auditing, not enforced |
| `ip_hash` | `String` | Yes | `NULL` | HMAC hash of client IP at login — stored for auditing, not enforced |
| `last_used_at` | `DateTime` | Yes | `NULL` | Updated on access token validation |
| `last_refreshed_at` | `DateTime` | Yes | `NULL` | Updated on token rotation |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Relationships:** `user` (many-to-one).

---

### `agents`

A registry of agent definitions synced from the agents service. Rows are upserted when the bridge fetches the agent catalog; they are not created by user action.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `slug` | `String` | No | — | UNIQUE — matches `BaseAgent.name` in the agents service |
| `name` | `String` | No | — | Human-readable label |
| `description` | `String` | No | — | Shown in agent picker UI |
| `icon` | `String` | No | — | Icon identifier resolved by the UI |
| `version` | `String` | Yes | `NULL` | Semver string; nullable for legacy agents |
| `is_active` | `Boolean` | No | `true` | Inactive agents are hidden from the picker |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Relationships:** `conversations` (one-to-many, cascade delete).

---

### `user_preferences`

One row per user, created on first access. The `tools` JSON column stores enabled/disabled tool preferences as a dict; the application deduplicates entries by `(server_id, tool_name)` key before writing.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `user_id` | `String` | No | — | FK → `users.id` CASCADE, UNIQUE (`uq_user_preferences_user_id`), INDEXED |
| `tools` | `JSON` | No | `{}` | Tool preference dict — `{disabled: [{serverId, toolName}]}` |
| `prefers_agentic_chat` | `Boolean` | No | `false` | Whether the user prefers agentic (tool-enabled) chat by default |
| `suggestions_enabled` | `Boolean` | No | `true` | Whether the UI shows suggested follow-up questions |
| `voice_mode_voice` | `String` | No | `"alloy"` | Selected TTS voice |
| `voice_mode_language` | `String` | No | `"english"` | Selected voice mode language |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Table constraint:** `UniqueConstraint("user_id", name="uq_user_preferences_user_id")`.

**Relationships:** `user` (many-to-one back-reference).

---

### `conversations`

The top-level container for a message thread. A conversation belongs to one user and one agent. The `forked_parent_id` / `forked_message_id` pair records the branching origin when a conversation is forked from a specific message in another conversation.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `user_id` | `String` | No | — | FK → `users.id` CASCADE, INDEXED |
| `agent_id` | `String` | No | — | FK → `agents.id` CASCADE, INDEXED — **last-used pointer**: updated to the agent of each new run. Per-message attribution lives on `messages.agent_id`; this reflects the agent of the most recent message. |
| `forked_parent_id` | `String` | Yes | `NULL` | FK → `conversations.id` SET NULL, INDEXED — origin conversation when forked |
| `forked_message_id` | `String` | Yes | `NULL` | FK → `messages.id` SET NULL, INDEXED — origin message when forked |
| `active_assistant_message_id` | `String` | Yes | `NULL` | FK → `messages.id` SET NULL, INDEXED — points at the AI message that is currently being streamed; `post_update=True` to avoid the circular insert ordering with `messages` |
| `agent_name` | `String` | Yes | `NULL` | Denormalized agent name at conversation creation time (survives agent renames) |
| `title` | `String` | Yes | `NULL` | Auto-generated or user-set title; max 200 characters |
| `is_private` | `Boolean` | No | `false` | Private conversations are excluded from shared exports and search results |
| `is_archived` | `Boolean` | No | `false` | Soft-delete; archived conversations are hidden from the default list |
| `archived_at` | `DateTime` | Yes | `NULL` | Set when `is_archived` flips to `true` |
| `is_reported` | `Boolean` | No | `false` | Set when a report is filed |
| `reported_at` | `DateTime` | Yes | `NULL` | Set when `is_reported` flips to `true` |
| `last_message_preview` | `String` | Yes | `""` | Truncated last message content for sidebar display |
| `last_message_at` | `DateTime` | No | `func.now()` | Updated on new message; drives sidebar sort order |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Relationships:** `user`, `agent`, `messages` (ordered by `created_at ASC`, cascade delete), `report` (one-to-one, cascade delete), `active_assistant_message` (many-to-one, `post_update=True`).

---

### `messages`

Every chat turn — both user and AI — is a row in this table. The `parent_message_id` self-reference enables branching: when a user edits a message or retries an AI response, a new sibling row is created under the same parent rather than overwriting the existing row.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `conversation_id` | `String` | No | — | FK → `conversations.id` CASCADE, INDEXED |
| `parent_message_id` | `String` | Yes | `NULL` | FK → `messages.id` SET NULL, INDEXED — self-reference for branching |
| `sender` | `MessageSenderEnum` | No | — | `"user"` or `"ai"` (Postgres enum `message_sender_enum`) |
| `content` | `Text` | Yes | `NULL` | Message body; `NULL` for placeholder rows created before inference completes |
| `liked` | `Boolean` | Yes | `NULL` | Three-state: `NULL` = not rated, `true` = liked, `false` = disliked |
| `agent_id` | `String` | Yes | `NULL` | FK → `agents.id` SET NULL, INDEXED — the agent that produced this message. Set on AI run messages; `NULL` on user messages. Per-message attribution lets one conversation mix agents. |
| `agent_name` | `String` | Yes | `NULL` | Denormalized agent label captured at run time, so a deactivated/removed agent still renders in the message action bar (mirrors `conversations.agent_name`). |
| `reasoning_steps` | `JSON` | Yes | `NULL` | Array of thought strings accumulated during inference |
| `reasoning_time_seconds` | `Integer` | Yes | `NULL` | Thinking duration in whole seconds |
| `input_tokens` | `Integer` | Yes | `NULL` | Per-message input tokens, summed across all model calls + sub-agents in the turn (true billed consumption — each call re-sends context). AI-run messages only; `NULL` on user/historical rows. Added in migration `0004`. |
| `output_tokens` | `Integer` | Yes | `NULL` | Per-message output tokens, summed across the turn. AI-run messages only; `NULL` otherwise. Migration `0004`. |
| `checkpoint_thread_id` | `String` | Yes | `NULL` | INDEXED (`ix_messages_checkpoint_thread_id`) — the per-branch LangGraph thread this run resumed from / created in the agents-service durable checkpointer. Shared across all runs on a branch; minted fresh on edit/retry (copy-on-fork). Looked up to resolve a branch's thread and to reap a conversation's threads on delete. Set on AI-run messages; `NULL` on user/pre-migration rows. Added in migration `0007`. |
| `checkpoint_id` | `String` | Yes | `NULL` | The durable checkpoint head this run committed, captured from the terminal `CHECKPOINT_COMMITTED` AG-UI event. Used by edit/retry to fork from the right point. AI-run messages only; `NULL` otherwise. Migration `0007`. |
| `is_error` | `Boolean` | No | `false` | `true` when the inference run that produced this message failed |
| `error_message` | `Text` | Yes | `NULL` | Error detail when `is_error=true` |
| `raw_events` | `JSON` | Yes | `NULL` | The **full per-run AG-UI event log** (every event, seq-stamped, with consecutive text/tool-args deltas coalesced and oversized `TOOL_CALL_RESULT` content truncated + flagged). The UI replays this into the rendered run timeline — including the agent plan (from `PLAN_SNAPSHOT` events) and sub-agent activity (from `SUBAGENT_EVENT`), which are no longer persisted as their own columns. Rows persisted before the timeline rebuild contain only `CUSTOM` events — the UI's legacy fold covers them. |
| `streaming_status` | `String` | Yes | `NULL` | INDEXED (partial, `IS NOT NULL`) — `"queued"`, `"running"`, `"cancelling"`, `"completed"`, `"cancelled"`, `"failed"`. `NULL` on user messages and on AI messages that were not produced by a detached run. |
| `streaming_message_path` | `JSON` | Yes | `NULL` | Ordered list of message IDs representing the branch path the agent saw as history when the run was started |
| `streaming_enabled_tools` | `JSON` | Yes | `NULL` | Snapshot of `{serverId, toolName}` tool preferences captured at run start |
| `streaming_started_at` | `DateTime` | Yes | `NULL` | Run creation timestamp; sorts the run list endpoint |
| `streaming_completed_at` | `DateTime` | Yes | `NULL` | Set on terminal status transition |
| `streaming_cancel_requested_at` | `DateTime` | Yes | `NULL` | Set when the client requests cancellation |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Relationships:** `conversation` (many-to-one), `attachments` (one-to-many, ordered by `created_at ASC`, cascade delete).

**The assistant message is the inference run.** There is no separate `inference_runs` table — the `streaming_*` columns on the AI `MessageTable` row carry the full run lifecycle. `InferenceRunRuntime` keeps the seq-stamped coalesced event log plus the flat aggregates (`content`, `reasoning_steps`, `plan`, `subagents`) in memory during the stream; on terminal write, those values plus the final `streaming_status` are persisted to the same row in a single transaction. The `InferenceRunOut` API shape is built from the row by `build_run_out_from_message(...)`, and `MessageOut` exposes `streaming_status` as `streamingStatus` so the client's Done sentinel can distinguish completed/cancelled/failed on hydration. Note the storage implication of the full log: `raw_events` now grows with run length (text and sub-agent activity are stored once in the log, with delta coalescing and tool-result truncation keeping it bounded); no DDL changed — the column was already `JSON`.

**Partial indexes on `messages`:**

```sql
CREATE UNIQUE INDEX uq_messages_one_active_stream_per_conversation
  ON messages (conversation_id)
  WHERE streaming_status IN ('queued', 'running', 'cancelling');

CREATE INDEX ix_messages_streaming_status
  ON messages (streaming_status)
  WHERE streaming_status IS NOT NULL;
```

The unique index enforces at most one active stream per conversation at the database level — a concurrent attempt to start a second run while one is still active raises a unique-constraint violation. The non-unique index supports the run-list endpoint's filter on `streaming_status IN (...)` without scanning the full table (only ~the active and recently-terminal AI rows are indexed).

A third, plain (non-partial) index `ix_messages_checkpoint_thread_id` on `checkpoint_thread_id` (migration `0007`) supports resolving a branch's durable LangGraph thread and reaping a conversation's checkpoint threads on delete.

---

### `attachments`

Metadata for every file attached to a message. The binary data lives in the `blobs` table; `attachments` is the linkage record. One message can have up to 10 attachments (validated by Pydantic, not a DB constraint). Total size per message is capped at 25 MB.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `message_id` | `String` | No | — | FK → `messages.id` CASCADE, INDEXED |
| `blob_id` | `String` | Yes | `NULL` | FK → `blobs.id` CASCADE, INDEXED — `NULL` only during a brief window between attachment record creation and blob upload |
| `file_name` | `String` | No | — | Original filename from upload |
| `mime_type` | `String` | No | — | MIME type declared by the uploader |
| `size_bytes` | `Integer` | Yes | `NULL` | File size in bytes |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Relationships:** `message` (many-to-one), `blob` (one-to-one, cascade delete, `single_parent=True`).

---

### `blobs`

Raw binary storage for file attachments. Stored in the database rather than an external object store. Each blob belongs to exactly one attachment (`single_parent=True`).

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `data` | `LargeBinary` | No | — | Raw file bytes |
| `created_at` | `DateTime` | No | `func.now()` | No `updated_at` — blobs are immutable after write |

**Relationships:** `attachment` (one-to-one back-reference).

---

### `conversation_reports`

A user-submitted flag on a conversation. At most one report per conversation is allowed (unique constraint). The `status` field is a free-form string — currently only `"open"` is written by the application; resolution workflow is manual.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `conversation_id` | `String` | No | — | FK → `conversations.id` CASCADE, UNIQUE (`uq_conversation_reports_conversation_id`), INDEXED |
| `user_id` | `String` | No | — | FK → `users.id` CASCADE, INDEXED — the reporter |
| `message_id` | `String` | Yes | `NULL` | FK → `messages.id` SET NULL, INDEXED — optional specific message flagged |
| `reason` | `String` | No | — | Short category; max 120 characters |
| `details` | `Text` | Yes | `NULL` | Optional free-form description; max 2000 characters |
| `status` | `String` | No | `"open"` | `"open"` on creation; updated manually by administrators |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

**Table constraint:** `UniqueConstraint("conversation_id", name="uq_conversation_reports_conversation_id")`.

---

### `conversation_shares`

A shareable link to a conversation snapshot. The `snapshot_json` column contains a point-in-time copy of the conversation and its messages — independent of the live data so the share remains stable even if the original conversation changes.

| Column | Type | Null | Default | Notes |
| --- | --- | --- | --- | --- |
| `id` | `String` | No | `gen_uuid()` | PK |
| `token` | `String` | No | — | UNIQUE, INDEXED — URL-safe random token; the public identifier for the share link |
| `conversation_id` | `String` | No | — | FK → `conversations.id` CASCADE, INDEXED |
| `owner_user_id` | `String` | No | — | FK → `users.id` CASCADE, INDEXED |
| `snapshot_until_message_id` | `String` | Yes | `NULL` | FK → `messages.id` SET NULL, INDEXED — last message included in the snapshot; `NULL` means all messages at share time |
| `title` | `String` | Yes | `NULL` | Share-specific title (may differ from conversation title) |
| `snapshot_json` | `JSON` | No | — | Full frozen snapshot: conversation metadata + message array |
| `is_active` | `Boolean` | No | `true` | Set to `false` when revoked |
| `revoked_at` | `DateTime` | Yes | `NULL` | Set when `is_active` flips to `false` |
| `expires_at` | `DateTime` | Yes | `NULL` | INDEXED — optional expiry; `NULL` means no expiry |
| `created_at` | `DateTime` | No | `func.now()` | — |
| `updated_at` | `DateTime` | No | `func.now()` | `onupdate=func.now()` |

---

## Constraint and Index Reference

### Partial Indexes

| Index name | Table | Column | WHERE clause | Purpose |
| --- | --- | --- | --- | --- |
| `uq_messages_one_active_stream_per_conversation` | `messages` | `conversation_id` | `streaming_status IN ('queued', 'running', 'cancelling')` | At most one active streaming AI message per conversation (UNIQUE) |
| `ix_messages_streaming_status` | `messages` | `streaming_status` | `streaming_status IS NOT NULL` | Fast filter on the run-list endpoint without scanning user/non-streaming rows |

### Named Unique Constraints

| Constraint name | Table | Column |
| --- | --- | --- |
| `uq_user_preferences_user_id` | `user_preferences` | `user_id` |
| `uq_conversation_reports_conversation_id` | `conversation_reports` | `conversation_id` |

### Implicit Unique Constraints (column-level)

| Table | Column(s) |
| --- | --- |
| `agents` | `slug` |
| `users` | `username`, `vault_user_id`, `email` |
| `sessions` | `access_token_hash`, `refresh_token_hash` |
| `conversation_shares` | `token` |

---

## Foreign Key Cascade Behaviour

| Child column | Parent | On delete |
| --- | --- | --- |
| `user_preferences.user_id` | `users.id` | CASCADE |
| `sessions.user_id` | `users.id` | CASCADE |
| `conversations.user_id` | `users.id` | CASCADE |
| `conversations.agent_id` | `agents.id` | CASCADE |
| `conversations.forked_parent_id` | `conversations.id` | SET NULL |
| `conversations.forked_message_id` | `messages.id` | SET NULL |
| `conversations.active_assistant_message_id` | `messages.id` | SET NULL |
| `messages.conversation_id` | `conversations.id` | CASCADE |
| `messages.parent_message_id` | `messages.id` | SET NULL |
| `messages.agent_id` | `agents.id` | SET NULL — deleting an agent never deletes message history; `agent_name` preserves the label |
| `conversation_reports.conversation_id` | `conversations.id` | CASCADE |
| `conversation_reports.user_id` | `users.id` | CASCADE |
| `conversation_reports.message_id` | `messages.id` | SET NULL |
| `conversation_shares.conversation_id` | `conversations.id` | CASCADE |
| `conversation_shares.owner_user_id` | `users.id` | CASCADE |
| `conversation_shares.snapshot_until_message_id` | `messages.id` | SET NULL |
| `attachments.message_id` | `messages.id` | CASCADE |
| `attachments.blob_id` | `blobs.id` | CASCADE |

**SET NULL semantics** — foreign keys that SET NULL on delete preserve referential history without blocking the delete. For example, deleting a message sets `messages.parent_message_id = NULL` on its children (orphaning their branch chain) and sets `conversations.forked_message_id = NULL` (the fork record survives, but the origin marker is cleared).

---

## Notable Design Decisions

### Message Branching via Self-Reference

`messages.parent_message_id` is a self-referential FK. Every message in a conversation has exactly one parent (or `NULL` for the root). When a user edits a message or retries an AI response, a new sibling row is created under the same parent — the existing row is never overwritten. The UI selects a path through this tree by traversing `parent_message_id` chains; the `streaming_message_path` column on the AI `messages` row stores the selected branch as an ordered list of IDs captured at inference start.

There is no `position` or `order` column — the canonical ordering within a branch is `created_at ASC`.

### Denormalized `agent_name` on Conversations

`conversations.agent_name` mirrors `conversations.agent_id` as a denormalized last-used label. Per-message attribution is the source of truth: each AI message carries its own `agent_id` + `agent_name`, so a conversation can mix agents and every message renders the agent that produced it. The conversation-level pair is updated to the latest run's agent (used for the sidebar/header default and the picker seed on conversation switch).

### `active_assistant_message_id` and `post_update`

`conversations.active_assistant_message_id` creates a circular reference with `messages` (the `messages.conversation_id` FK points back at `conversations`). SQLAlchemy's `post_update=True` on the `active_assistant_message` relationship breaks the insert-ordering deadlock by issuing the `conversations` UPDATE in a second round-trip after the initial AI placeholder `INSERT INTO messages`.

### `blobs` Has No `updated_at`

Blobs are write-once. Once a file is uploaded, its binary content never changes. `blobs` therefore has only `id`, `data`, and `created_at`. The associated `attachments` metadata row may be updated (e.g., `blob_id` set after initial creation), but the blob itself is immutable.

### JSON Columns for AG-UI State

`messages.raw_events`, `messages.plan`, and `messages.subagents` store structured AG-UI event data as Postgres JSON. This avoids normalizing a rapidly-evolving event schema into relational tables. During streaming these columns are `NULL` — the live in-flight state lives in the per-task `InferenceRunRuntime` accumulator (process memory) and is mirrored into the durable Redis stream at `inference:run:{message_id}:events`. On terminal write, the accumulator's final state is persisted to the row's JSON columns in a single atomic commit alongside `streaming_status` and `streaming_completed_at`.

### `conversation_shares.snapshot_json` is a Frozen Copy

The share snapshot is stored entirely in `snapshot_json` — a point-in-time JSON copy of the conversation and its messages. Shared links remain stable and readable even if the original conversation is deleted. Accessing a share does not query `conversations` or `messages` at all.

---

## Application-Level Validation Constraints

These rules are enforced by Pydantic schemas, not database constraints:

| Rule | Limit |
| --- | --- |
| Max attachments per message | 10 |
| Max size per attachment | 25 MB |
| Max total attachments per message | 25 MB |
| Conversation title max length | 200 characters (truncated) |
| Report reason max length | 120 characters (truncated) |
| Report details max length | 2000 characters (truncated) |
| Tool preference list | Deduplicated by `(server_id, tool_name)` before write |

---

## Sharp Edges and Behavioral Notes

- **Schema changes go through Alembic migrations.** Edit the ORM model in `core/database.py`, then autogenerate + hand-review a migration in `migrations/versions/` (the ordered chain `0001_baseline` → `0007_…`). `alembic upgrade head` runs automatically on bridge startup. Never use `metadata.create_all`. See the CLAUDE.md "Schema Changes — Migration Workflow" section.

- **All IDs are UUIDs stored as `String`, not the native Postgres `UUID` type.** This avoids a SQLAlchemy/asyncpg type handling quirk but means the database does not reject malformed IDs at the storage layer — only application code validates UUID format.

- **`messages.content` is `NULL` for placeholder rows.** When inference starts, the AI message row is created immediately with `content=NULL`. It is populated only when `_finish_run()` commits the terminal state. Any query that joins messages and assumes `content IS NOT NULL` will silently miss in-flight rows.

- **`sessions` rows are never deleted.** Logout sets `revoked_at`; the row stays. Over time this table grows unbounded. There is no background job to purge old or expired sessions.

- **`user_preferences.tools` is a JSON blob, not a junction table.** The entire tools preference object is replaced on each update. Concurrent writes from two browser tabs produce a last-write-wins race. The `updated_at` column is the only write guard.

- **`conversation_shares.snapshot_json` is stored inline, not in `blobs`.** Large conversations with many messages produce large JSON values stored directly in the Postgres row. There is no size cap enforced by the schema.

- **The partial index conditions are string literals.** If the set of active status strings ever changes (e.g., a new `"pausing"` status is added), the `uq_messages_one_active_stream_per_conversation` and `ix_messages_streaming_status` WHERE clauses must be updated manually — they do not reference the application's `ACTIVE_RUN_STATUSES` constant.

- **`conversations.active_assistant_message_id` is SET NULL on message delete, not on streaming-status change.** The column is updated programmatically when the AI message transitions to a terminal `streaming_status`. A crash between terminal write and the `UPDATE conversations SET active_assistant_message_id = NULL` leaves the column pointing at a completed message until the next startup cleanup pass.

- **The durable in-flight event log is in Redis, not Postgres.** Chunks streamed during a run are appended to the Redis Stream `inference:run:{message_id}:events` via `RedisEventLog.append(...)`. Postgres only sees the terminal snapshot when `_finish_run()` commits. Killing the bridge mid-stream leaves the AI row in `streaming_status='running'`; the startup cleanup pass (`cleanup_orphaned_inference_runs`) transitions any abandoned rows to `failed`.

- **`users.email` allows `NULL` with a UNIQUE constraint.** Postgres treats each `NULL` as distinct, so multiple users with no email on record do not violate the constraint. The UNIQUE index only prevents two rows from sharing the same non-null email value.

---

## File Map

| Concept | File | What to look for |
| --- | --- | --- |
| All ORM model definitions | [src/dialogue_bridge/core/database.py](../../src/dialogue_bridge/core/database.py) | All `Table` classes, `__table_args__`, relationships |
| Partial indexes on `messages` | [src/dialogue_bridge/core/database.py](../../src/dialogue_bridge/core/database.py) | `Index("uq_messages_one_active_stream_per_conversation", ..., postgresql_where=...)`, `Index("ix_messages_streaming_status", ...)` |
| Pydantic request/response schemas | [src/dialogue_bridge/schemas/\_\_init\_\_.py](../../src/dialogue_bridge/schemas/__init__.py) | All `BaseModel` subclasses, attachment limits, enum literals |
| User upsert on login | [src/dialogue_bridge/core/database.py](../../src/dialogue_bridge/core/database.py) | `upsert_user_from_vault()` |
| Active run status set | [src/dialogue_bridge/utils/inference_runs.py](../../src/dialogue_bridge/utils/inference_runs.py) | `ACTIVE_RUN_STATUSES` |
| Session HMAC hashing | [src/dialogue_bridge/core/auth_session.py](../../src/dialogue_bridge/core/auth_session.py) | `_hash_token()` |
| Session cap enforcement | [src/dialogue_bridge/core/auth_session.py](../../src/dialogue_bridge/core/auth_session.py) | `_enforce_session_limit()` |
| Async engine and session factory | [src/dialogue_bridge/core/database.py](../../src/dialogue_bridge/core/database.py) | `engine`, `SessionLocal`, `get_db()` |
| Conversation access control | [src/dialogue_bridge/router/conversations.py](../../src/dialogue_bridge/router/conversations.py) | `validate_convId_full()` — user\_id + conversation\_id filter |
| Share snapshot write | [src/dialogue_bridge/utils/share_export.py](../../src/dialogue_bridge/utils/share_export.py) | `build_snapshot_json()` |
